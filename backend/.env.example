# Copia este archivo a .env y reemplaza los valores por los tuyos.
# Ejemplo de variables de entorno para el backend

# MongoDB local - Cambia "social network" por el nombre exacto de tu BD en Mongo
MONGO_URI=mongodb://127.0.0.1:27017/SOCIAL-NET258
PORT=3000
JWT_SECRET=tu_jwt_secreto_aqui

# Si usas MongoDB Atlas, pon la URL de conexión completa:
# MONGO_URI=mongodb+srv://usuario:contraseña@cluster0.abcde.mongodb.net/dbname?retryWrites=true&w=majority
